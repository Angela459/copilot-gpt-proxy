[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "copilot-gpt-proxy"
version = "0.1.1"
description = "OpenAI-compatible compatibility proxy for GitHub Copilot and GPT tool calls"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "Yixing Lao" },
]
keywords = ["github-copilot", "gpt", "proxy", "llm", "openai-compatible", "tool-calls"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: Proxy Servers",
    "Topic :: Software Development",
]
dependencies = [
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "black==24.2.0",
    "pre-commit>=3.0.0",
    "ruff>=0.3.0",
]

[project.scripts]
copilot-gpt-proxy = "copilot_gpt_proxy.server:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["copilot_gpt_proxy*"]

[tool.black]
target-version = ["py310"]
